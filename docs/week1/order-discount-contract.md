# 주문 쿠폰 할인 — 설계 기록

## 0. 기준 문장

> (기준 문장을 그대로 옮기고, 출처를 적으세요)

## 1. 기존 동작 관찰 (Example API)

> `ExampleV1ApiE2ETest`와 `ContractClassificationTest`로 확인한, 이미 있는 `/api/v1/examples/{id}`의 계약을 채워보세요. 이게 4장(외부 API 계약)을 정할 때 기준선이 됩니다.

| 입력 | HTTP status | `meta.result` | `meta.errorCode` | `data` 유무 | 확인 방법 |
|---|---|---|---|---|---|
| 존재하는 숫자 ID | | | | | |
| 숫자가 아닌 ID | | | | | |
| 존재하지 않는 숫자 ID | | | | | |
| 미매핑 URL | | | | | |

> (관찰에서 눈에 띄는 점이 있다면 한두 줄로 적어보세요 — 예: 두 가지 다른 상황이 같은 응답으로 합쳐지는지)

## 2. 요구사항 분류

### 2-1. 확인된 사실 (제품 약속)

> 기준 문장에서 직접 근거를 찾을 수 있는 것만. 2개.

| # | 내용 | 근거 |
|---|---|---|
| F-1 | | |
| F-2 | | |

### 2-2. 이번 과제에서만 적용하는 실습용 전제

> 실제 정책으로 확정된 게 아니라 이번 실습 범위를 좁히기 위한 임시 조건. 4개. 각각 "틀렸을 때 무엇을 되돌려야 하는지"도 적으세요.

| # | 내용 | 영향 범위 (틀렸을 때 되돌릴 지점) |
|---|---|---|
| P-1 | | |
| P-2 | | |
| P-3 | | |
| P-4 | | |

### 2-3. 제품 담당자에게 확인할 질문

> 3~5개. 각 질문이 답에 따라 **사용자 응답 / 저장 데이터 / 권한** 중 무엇을 바꾸는지 반드시 연결하세요.

| # | 질문 | 답에 따라 달라지는 것 |
|---|---|---|
| Q-1 | | |
| Q-2 | | |
| Q-3 | | |

### 2-4. 출처 없는 제안

> AI나 도구가 제안했지만 기준 문장·실습 전제·기존 코드 관찰 어디에도 근거가 없는 내용이 있다면 `보류`/`거절`+이유로 적으세요. 없다면 "해당 없음"이라고 쓰세요.

## 3. 불변식과 반례 (`INV-001` ~ `INV-006`)

> 반드시 포함: 소유권, 쿠폰 개수, 금액 경계, 확정 결과 보존, 중복 요청(멱등), 만료 경계. 각 규칙에 반례를 하나씩 연결하세요.

| 규칙 ID | 출처 구분 | 규칙 내용 | 깨지는 입력(반례) | 기대 결과 | 책임 후보 |
|---|---|---|---|---|---|
| `INV-001` | | | | | |
| `INV-002` | | | | | |
| `INV-003` | | | | | |
| `INV-004` | | | | | |
| `INV-005` | | | | | |
| `INV-006` | | | | | |

## 4. 외부 API 계약과 내부 책임 경계

### 4-1. 외부 API 계약

- **Method / Path**:
- **사용자 식별**:
- **Request**:
- **성공 응답**:
- **오류별 외부 의미**:

| 상황 | HTTP | `meta.result` | `meta.errorCode` | `data` | 요청자의 다음 행동 |
|---|---|---|---|---|---|
| | | | | | |

> 기본 시나리오(`POST /api/v1/orders/{orderId}/discount`, body에 `couponId`)와 다르게 설계했다면 차이와 이유를 적으세요.

### 4-2. 규칙별 책임

| 책임 | 보장해야 하는 상태/행동 | 관련 규칙 |
|---|---|---|
| 주문 상태 확인 | | |
| 쿠폰 사용 가능 여부 확인 | | |
| 할인 계산 | | |
| 결과 저장 | | |

### 4-3. 세 가지 설계 결정

각 결정마다 **선택한 방식 / 버린 대안 / 이유**를 적으세요.

**결정 1. 오류를 어떻게 구분하고 표현할 것인가**
- 선택한 방식:
- 버린 대안:
- 이유:

**결정 2. snapshot 저장 vs 재계산**
- 선택한 방식:
- 버린 대안:
- 이유:

**결정 3. 조회 → 계산 → 저장까지의 호출 순서를 어느 경계가 숨길 것인가**
- 선택한 방식:
- 버린 대안:
- 이유:

## 5. 다음 단계

> 이 문서에서 답이 안 나온 질문(2-3)을 언제/누구에게 확인할지, `ContractClassificationTest`로 검증할 부분이 남았는지 적으세요.
